include ./header.pug 
include ./nav.pug 
include ./sidebar.pug 
  .container
    .row.mt-3
      each action in actions
        .col-md-4
          a(href=action.link).card.text-dark.text-center.mb-3
            .card-body
              i(class=action.icon + ' fa-fw fa-6x').mb-3
              h1=action.name

include ./footer.pug 